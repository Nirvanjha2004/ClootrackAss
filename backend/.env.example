# Django Settings
# Comma-separated list of allowed hosts (e.g., localhost,127.0.0.1,yourdomain.com)
ALLOWED_HOSTS=localhost,127.0.0.1,testserver

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database_name
# Example: postgresql://ticketuser:password123@localhost:5432/ticketdb
DATABASE_URL=postgresql://postgres:postgres@db:5432/ticketdb

# OpenAI API Configuration
# Required for LLM-based ticket classification
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# CORS Configuration
# Comma-separated list of allowed origins for frontend
# Example: http://localhost:3000,http://127.0.0.1:3000,https://yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Django Secret Key (generate a new one for production)
# You can generate one using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure--b&@jru5dqr$=bi6+xdl75zlpu@4mo5mg!7=4m^l7kbfh28=bv

# Debug Mode (set to False in production)
DEBUG=True
